只读复核完成；使用内存 Store／文件系统替身验证，未修改文件、未运行落盘测试。

1. **已修**：撤销快照取自落子前，与 `runLoop` 的回合边界语义一致；连续撤销、撤销后立即落子并停在连锁决策均通过，磁盘快照没有混入半回合状态（`mcp/src/game.mjs:61、71、158–162`）。

2. **已修**：模拟后的 `s.turn.player` 已是下一次 `runTurn` 要检查的玩家，额外回合时仍是自己；108 组两至四人及使者耗尽组合与引擎一致（`mcp/src/view.mjs:152–154`；`online/engine.js:526、532–533`）。

3. **改法有问题**：推进、印章收益和 herald 可选动作已纳入分类，被挡提示也兼容到顶／禁行格，但 `hot()` 漏掉了新补出的终局结果，出现终局落点被归为布局的交互问题（`mcp/src/view.mjs:180、194`）。

4. **改法有问题**：英文已明确额外回合只是记账，中文却写成“额外回合全部结算完之后”，可能误导终局时点（`mcp/data/handbook.en.md:38`；`mcp/data/handbook.zh.md:29`）。

5. **改法有问题**：规则段与 `inspect` 已改为当前接触判断，但中文策略仍称“永远放弃”，落点说明仍称城镇被该群消耗，表述没有统一（`mcp/data/handbook.zh.md:53`；`mcp/src/view.mjs:163、223`）。

6. **改法有问题**：临时文件加 rename 已实现，模拟替换失败也保住了旧档，但 `saveError` 只进入 `afterText`，开局和 `get_state` 仍不报告（`mcp/src/game.mjs:205–207`；`mcp/src/view.mjs:108`；`mcp/src/index.mjs:72、80`）。

7. **改法有问题**：再次终局会正确追加 `rev:2, tag:"end"`，但终局撤销后尚未再次结束时，没有失效记录，按最新记录读取仍会采用已撤销结果（`mcp/src/game.mjs:158–161、176–177`）。

8. **已修**：已有旧引擎且重建失败时，会明确输出使用过期引擎及失败诊断（`mcp/src/engine.mjs:23–25`）。

9. **改法有问题**：已增加参考资料声明及逐行引用，但复盘仍随手册拼进同一条 `role:user` 开局指令，未完成单独提供的隔离（`mcp/src/index.mjs:28–29、193–195`）。

10. **已修**：按 `mtimeMs` 升序后取最后五份；模拟六份文件验证了最新文件不会再被随机 ID 排序排除（`mcp/src/index.mjs:24、27`）。

本轮新增问题：

- 🟠 **终局落点被默认筛选隐藏**（`mcp/src/view.mjs:154、194–197`）。最后一枚使者落在额外回合农夫格、没有其他收益时，新增判定得到 `ends=true、extra=0`，但 `hot()` 返回假；只要另有得分招法，默认列表就不显示该终局落点。已复现 `12,1` 在全部列表显示 `ENDS THE GAME`，默认列表缺失。**改法：将 `f.ends` 纳入显著效果分类。**

- 🟠 **中文新增措辞暗示终局前还执行额外回合**（`mcp/data/handbook.zh.md:29`）。已有额外回合且本次落子达到 50 分时，引擎直接终局；复现为 51 分、仍有一个未执行额外回合。**改法：写成“本次落子及其连锁、成就结算完成后检查；额外回合仅累计，达到终局条件便不再执行”。**

- 🟢 **保存失败提示与保留旧档的新行为不符**（`mcp/src/view.mjs:108`）。已有有效存档而本次 rename 失败时，旧档仍可恢复，并非整局“无法在重启后保留”。**改法：提示“本次进度未保存，重启可能回退到上次成功保存的回合”。**

结论：暂不建议交付外部 MCP 客户端使用，仍需补齐上述未完成修复并解决新增的终局筛选问题。
