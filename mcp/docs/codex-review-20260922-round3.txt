只读复核完成；已对照第二轮快照，并通过内存 Store、文件系统替身及 SDK 消息格式校验，未修改文件。

| 第二轮条目 | 结果 | 一句依据 |
|---|---|---|
| #3 | **已修** | `hot()` 已纳入 `f.ends`，终局落点进入默认效果分类（`mcp/src/view.mjs:196`）。 |
| #4 | **已修** | 中英文均明确落子与连锁结算后检查终局，所得额外回合不再执行（`mcp/data/handbook.zh.md:29`；`mcp/data/handbook.en.md:38`）。 |
| #5 | **已修** | 双语策略及落点说明均改为“该使者仍邻接期间不能计分”，消除了永久消耗的表述（`mcp/data/handbook.zh.md:53`；`mcp/data/handbook.en.md:67`；`mcp/src/view.mjs:165`）。 |
| #6 | **已修** | `stateText` 与 `afterText` 共用保存失败提示，覆盖开局和查询状态（`mcp/src/view.mjs:77、109、113`）。 |
| #7 | **已修** | 终局撤销追加无 `result` 的 `tag:"undo"`，实测日志顺序为 `end → undo → end(rev:2)`（`mcp/src/game.mjs:159、177`）。 |
| #9 | **已修** | 指令正文使用不含复盘的手册，复盘作为独立 `type:"resource"` 消息附带，格式校验通过（`mcp/src/index.mjs:200、202`）。 |
| 新增①：终局落点被隐藏 | **已修** | 已复现 `12,1` 终局落点进入默认分类、不再归入布局（`mcp/src/view.mjs:196–198`）。 |
| 新增②：中文终局时点歧义 | **已修** | 已明确额外回合不再执行，与达到 50 分后直接终局的实测一致（`mcp/data/handbook.zh.md:29–30`）。 |
| 新增③：保存失败措辞失实 | **已修** | 提示已改为本回合未保存、重启可能回退，与替换失败仍保留旧档的行为一致（`mcp/src/view.mjs:113`）。 |

本轮改动引入的新问题：**无**。

结论：就本次复核范围而言，可以交付外部 MCP 客户端使用。
