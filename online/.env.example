# 复制为 .env 后填写；deploy.example.sh 与 docker-compose.yml 都读这里
CASC_OWNER_PIN=change-me-owner      # 房主口令：只有知道它的人能开房
CASC_SITE_PIN=change-me-site        # 站点配对码：朋友进站时输入；改了它=旧授权全部作废
CASC_MAX_ROOMS=8                    # 同时存在的房间上限
